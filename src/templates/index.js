import React from 'react'

import Layout from '../components/layout'

export default ({ pageContext }) => (
  <Layout pageContext={pageContext} color="red">
    <h2>Index Page</h2>
    This is a fancier layout just for the landing page.
  </Layout>
)
